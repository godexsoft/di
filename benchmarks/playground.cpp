#include <di.hpp>

#include <benchmark/benchmark.h>

// static void Benchmark_MyBench(benchmark::State &state) {
//     for(auto _ : state)
//         benchmark::DoNotOptimize(di::Services<A, B, C>());
// }
// BENCHMARK(Benchmark_MyBench);
